# OpenClaw Client Configuration
# Auto-generated by fomo-core provisioning service

instance:
  id: "{{client_id}}"
  name: "{{instance_name}}"
  company: "{{company_name}}"
  vertical: "{{company_vertical}}"

manager:
  name: "{{manager_name}}"
  owner: "{{owner_name}}"

channels:
  {{channels_config}}

gateway:
  port: {{health_check_port}}
  host: "0.0.0.0"

logging:
  level: "info"
  format: "json"

integrations:
  fomo_core:
    api_url: "{{fomo_core_api_url}}"
